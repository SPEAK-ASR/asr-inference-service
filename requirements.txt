# --- Web framework ---
fastapi>=0.115,<0.118
uvicorn[standard]>=0.30,<0.33

# --- Validation / config ---
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
python-dotenv>=1.0,<2

# --- ML stack (CPU/GPU compatible; CUDA wheel chosen by env if available) ---
# NOTE: For CUDA, install torch + torchaudio from the same PyTorch index URL, e.g.:
#   pip install --index-url https://download.pytorch.org/whl/cu121 "torch>=2.8,<3" "torchaudio>=2.8,<3"
# Diarization (pyannote.audio 4.x) also needs ffmpeg on PATH for torchcodec decoding.
torch>=2.8,<3
torchaudio>=2.8,<3
transformers>=4.44,<5
accelerate>=0.33
peft>=0.13
faster-whisper>=1.0,<2
numpy>=1.26,<3

# --- VAD (realtime segmentation; matches HF Space pattern) ---
silero-vad>=6,<7

# --- Speaker diarization (optional; toggle via POST /admin/diarization) ---
# 4.x + pyannote/speaker-diarization-community-1 (HF "Latest"); 3.1 is legacy on HF.
pyannote.audio>=4.0,<5

# --- Audio I/O ---
soundfile>=0.12

# --- Misc ---
typing-extensions>=4.10
